// JSON形式のデータ（オブジェクト）
let user = {
    "name": "山田太郎",
    "age": 30,
    "hobbies": ["読書", "映画", "旅行"]
};

// オブジェクトのプロパティにアクセス
console.log(user.name);  // "山田太郎"
console.log(user.hobbies[0]);  // "読書"

